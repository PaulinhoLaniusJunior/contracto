const loginBtn = document.getElementById('loginBtn');

loginBtn.addEventListener('click', () => {
    window.location.href = 'TELA DE LOGIN/index.html'; // Redireciona para a página de login
});
